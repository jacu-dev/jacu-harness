# Context admission eval

Ten fixture missions packed against a temp repository. Refusals were
inspected: each `required_overflow` named a required file larger than the
remaining budget. No refusal was a false positive on optional-only overflow.

| n | admit | degrade | refuse | notes |
|---|---|---|---|---|
| 10 | 6 | 2 | 2 | both refusals were required files over budget |

Coverage_bps on admits: 10000. Degrades recorded dropped_optional > 0.
