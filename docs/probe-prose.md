# Probe — prose lane (SDD-023)

Throwaway document. It exists to make a pull request whose diff is prose and
nothing else, so the `changed-code` classifier answers `prose-only`, `verify`
is skipped and the `prose` lane runs in its place.

This file is never merged.
